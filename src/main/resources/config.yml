server:
  host: 0.0.0.0
  port: 8080

api:
  enabled: true
  public: true
  require-token: false
  token: ""

cors:
  enabled: true
  allow-origin: "*"

cache:
  enabled: true
  save-json: true
  update-seconds: 60

messages:
  api-started: "Avatar API running on port %port%"
  api-disabled: "API Disabled"
  player-offline: "Player not online"
  skin-not-found: "Skin not found"

debug: false